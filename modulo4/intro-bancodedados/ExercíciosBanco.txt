INSERT INTO Actor (id, name, salary, birth_date, gender)
VALUES(
  "003", 
  "Fernanda Montenegro",
  300000,
  "1929-10-19", 
  "female"
);

INSERT INTO Actor (id, name, salary, birth_date, gender)
VALUES(
  "004", 
  "Ronald Santiago",
  400000,
  "1949-04-18", 
  "male"
);

INSERT INTO Actor (id, name, salary, birth_date, gender)
VALUES(
  "005", 
  "Bella Harmone",
  719333.33,
  "1990-03-26", 
  "female"
);


SELECT * from Actor WHERE gender = "female";
SELECT salary from Actor WHERE name = "Tony Ramos";
SELECT * from Actor WHERE gender = "invalid";
SELECT id, name, salary from Actor WHERE salary < 500000;

SELECT * FROM Actor
WHERE name NOT LIKE "A%" AND salary > 350000;

SELECT * FROM Actor
WHERE name LIKE "%g%" OR name LIKE "%G%";
SELECT * FROM Actor
WHERE 
(name LIKE "%g%" OR name LIKE "%G%" OR name LIKE "%a%" OR name LIKE "%A%") AND salary BETWEEN 350000 AND 900000;


a) O varchar é um tipo de string
O date representa literalmente data mesmo

B) Agora apareceu um resultado tanto de colunas como só com uma tabela


EXERCÍCIO 2
B)O erro que deu foi de que estava duplicando a key
C) "Correspondência de coluna não valoriza a contagem da linha um"
Eu suponho que seja pois falta algumas propriedades como tem na primeira coluna.

D) "nome do campo não tem padrão"
Eu suponho que porque faltou a propriedade 'name'

EXERCÍCIO 3
C) Deu que uma instância estava duplicada
E)Diz que ao procurar  não achou uma coluna nome


EXERCICIO 4
Procure o nome a ou j maior que 30000

